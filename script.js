$(document).ready(fuction(){
    /*This is exucuted after the DOM has been completely loaded */

    $('nav a,footer a.up').click(function(e){

        //If a link has been clicked, scroll the page to the link's
        target:
           $.scrollTO(this harsh || 0, 1500);
           e.preventDefault();
      });

  });
